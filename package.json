{
  "name": "@ismail-elkorchi/episteme",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Deterministic, agent-first extraction of structured knowledge.",
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ismail-elkorchi/episteme.git"
  },
  "bugs": {
    "url": "https://github.com/Ismail-elkorchi/episteme/issues"
  },
  "homepage": "https://github.com/Ismail-elkorchi/episteme",
  "files": [
    "src",
    "schema",
    "README.md",
    "package.json"
  ],
  "bin": {
    "episteme": "src/cli.js"
  },
  "scripts": {
    "snapshot": "node src/cli.js snapshot",
    "extract": "node src/cli.js extract",
    "chunk": "node src/cli.js chunk",
    "index": "node src/cli.js index",
    "diff": "node src/cli.js diff",
    "pipeline": "node src/cli.js pipeline",
    "extract:example": "node src/cli.js extract --manifest examples/manifest.json --out out --format json",
    "extract:md": "node src/cli.js extract --manifest examples/manifest.json --out out --format md",
    "manual-ingest": "node src/cli.js manual-ingest",
    "test:xml": "node tests/xml-extractor.test.js",
    "test:html-engine": "node tests/html-engine.contract.test.js",
    "test:html": "node tests/html-extractor.test.js",
    "test:html:golden": "node tests/html-golden.test.js",
    "test:pdf": "node tests/pdf-extractor.test.js",
    "test": "npm run prepare:parser-stack && npm run test:xml && npm run test:html-engine && npm run test:html && npm run test:html:golden && npm run test:pdf",
    "prepare:parser-stack": "node scripts/install/prepare-parser-stack-deps.mjs",
    "check": "npm run test",
    "check:holdout": "node tests/holdout/xml-extractor.holdout.test.js",
    "check:denylist": "node scripts/security/check-denylist.mjs",
    "check:deno": "npm run prepare:parser-stack && deno run --allow-read --node-modules-dir=manual tests/xml-extractor.test.js && deno run --allow-read --node-modules-dir=manual tests/html-engine.contract.test.js && deno run --allow-read --node-modules-dir=manual tests/html-extractor.test.js && deno run --allow-read --node-modules-dir=manual tests/html-golden.test.js && deno run --allow-read --node-modules-dir=manual tests/pdf-extractor.test.js",
    "check:bun": "npm run prepare:parser-stack && bun tests/xml-extractor.test.js && bun tests/html-engine.contract.test.js && bun tests/html-extractor.test.js && bun tests/html-golden.test.js && bun tests/pdf-extractor.test.js",
    "package:check": "node scripts/verify-package.mjs",
    "check:ci": "npm run prepare:parser-stack && npm run check && npm run check:holdout && npm run check:denylist && npm run package:check"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "@ismail-elkorchi/css-parser": "^0.1.1",
    "@ismail-elkorchi/html-parser": "^0.1.1",
    "@ismail-elkorchi/xml-parser": "^0.1.1",
    "pdfjs-dist": "^4.10.38"
  },
  "license": "MIT"
}
